# My Stock V2 - Implementation Plan (plan.md)

## 1. 프로젝트 마일스톤 (Milestones)

### Phase 1: 기본 인프라 구축 및 11개 차트 시각화 (완료)
- DART Open API를 활용한 분기/연간 재무제표 원천 데이터 수집.
- FastAPI를 활용한 캐싱 및 정적 파일(HTML/JS) 서빙.
- ECharts를 이용한 11개 핵심 재무/수익성 지표 차트 구현 및 찌그러짐 현상(Resize bug) 해결.

### Phase 2: 가치평가 모델 (DCF & S-RIM) 도입 (완료)
- 향후 5년 추정 주주이익 DCF 계산기 프론트엔드 연동.
- 사경인 S-RIM 적정주가 모델 산식 반영 및 요구수익률 10% 기반 시나리오(w)별 밸류에이션 UI 연동.
- 야후 파이낸스 라이브러리 제거 및 순수 DART 데이터 + 네이버 금융 크롤링 기반 파이프라인 경량화.

### Phase 3: 데이터 파싱 로직 전면 리팩토링 및 안정화 (현재 진행 중)
- **배경**: 부분 문자열 매칭(`in`)으로 인한 치명적 데이터 덮어쓰기 오류(예: '영업이익'이 '중단영업이익'에 의해 덮어씌워짐) 발견.
- **목표**: 
  1. 공식 문서화(`spec.md`, `plan.md`) 완료. (현재 단계)
  2. `data_processor.py`에서 표준 `account_id` 기반의 엄격한 매칭 룰셋 적용.
  3. 프론트엔드(`chart_render.js`) 방어 로직 강화 및 `null` 시리얼라이제이션 검증.
  4. 과거 오염된 엑셀 캐시 청소 및 통합 테스트 수행.

### Phase 4: 향후 고도화 로드맵 (Future Work)
- **DB 전면 전환**: 현재 엑셀 파일 저장 방식(`my-list/*.xlsx`)에서 벗어나, SQLite 기반의 시계열 재무 DB로 완전 통합.
- **다중 기업 비교**: 2개 이상의 기업을 검색하여 동일한 차트 위에 오버랩(Overlap)하여 직관적으로 실적을 비교할 수 있는 UI 구축.
- **알림 기능**: 주요 분기 실적 발표 시 자동 스크래핑 및 분석 요약본 제공 로직.

---

## 2. 현재 진행 중인 작업 (Phase 3 상세 계획)
*사용자의 '순차적 리팩토링' 요청에 따라 아래 순서대로 진행합니다.*

- [x] **Step 1. 공식 문서화**: `spec.md` 및 `plan.md` 작성.
- [x] **Step 2. 백엔드 핵심 파싱 로직 교체**: `data_processor.py` 내 `parse_financial_data()` 함수 전면 재작성 (account_id 우선순위 및 필터링 규칙 도입).
- [x] **Step 3. 프론트엔드 UI 안정화**: `chart_render.js`의 `valueFormatter` 및 데이터 매핑 로직 다듬기.
- [x] **Step 4. 통합 테스트**: 캐시 비우기 및 서버 재시작 후 완벽하게 그려지는 11개 차트 최종 확인.
