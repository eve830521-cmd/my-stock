# My Stock V2 - Product Specification (spec.md)

## 1. 개요 (Overview)
본 문서는 'My Stock V2' 주식 분석 웹 애플리케이션의 핵심 아키텍처, 제공 기능, 데이터 파이프라인 및 가치평가(DCF, S-RIM) 산식을 정의하는 단일 진실 공급원(Single Source of Truth)입니다.

## 2. 시스템 아키텍처 (Architecture)
### 2.1. Backend (Python / FastAPI)
- **Framework**: FastAPI (API 라우팅 및 정적 파일 서빙)
- **Data Source**: DART Open API (재무제표 데이터), 네이버 금융 웹 스크래핑 (현재 주가)
- **Database**: SQLite (`database.db` - DART 기업 코드 및 API 응답 캐싱)
- **Data Processing**: `data_processor.py` (Pandas 기반 TTM 및 Annual 데이터 변환, Excel 캐싱)

### 2.2. Frontend (HTML / JS / CSS)
- **UI/UX**: 다크 모드 기반 단일 페이지 애플리케이션 (SPA)
- **Chart Library**: ECharts (11개 재무 및 밸류에이션 차트)
- **Valuation Logic**: 클라이언트 사이드(JS) 동적 시뮬레이션 (`dcf_valuation.js`)

## 3. 핵심 산식 및 파생 지표 (Formulas)
### 3.1. 기본 재무 지표
- **유통주식수** = `당기순이익 / EPS` (DART 데이터 기반 역산)
- **시가총액** = `현재 주가(네이버 금융) * 유통주식수`
- **FCF (잉여현금흐름)** = `영업활동현금흐름 - |CAPEX|`
- **순단기금융자산** = `단기금융자산 - 단기금융부채`
- **투하자본(IC)** = `자본총계 + 단기금융부채 - 현금및현금성자산`
- **NOPAT (세후영업이익)** = `영업이익 * (1 - 법인세율)`

### 3.2. 수익성 및 건전성 지표
- **ROE (자기자본이익률)** = `(지배순이익 / 자본총계) * 100`
- **ROIC (투하자본수익률)** = `(NOPAT / 투하자본) * 100`
- **이자보상배율** = `영업이익 / 이자비용` (0 또는 음수일 경우 NaN 처리)
- **PER** = `시가총액 / 지배순이익` (음수일 경우 NaN 처리)
- **PBR** = `시가총액 / 자본총계`

## 4. 가치평가 모델 (Valuation Models)
### 4.1. 주주이익 DCF (Discounted Cash Flow)
- **입력 변수**: 매출액 성장률(낙관/중립/비관), 영업이익률, 법인세율, 할인율(WACC), 영구성장률(Terminal Growth Rate).
- **산식**:
  1. 향후 5년 추정 FCF = `추정 매출액 * 추정 영업이익률 * (1 - 법인세율)`
  2. 영구가치(Terminal Value) = `5년차 FCF * (1 + 영구성장률) / (할인율 - 영구성장률)`
  3. 기업가치 = `Σ(할인된 5년 FCF) + 할인된 영구가치`
  4. 주주가치 = `기업가치 + 순단기금융자산`
  5. 적정주가 = `주주가치 / 유통주식수`

### 4.2. S-RIM (사경인 적정주가 모델)
- **입력 변수**: 지배주주자본(최근 연도), 예상 ROE (과거 3년 평균 기반), 요구수익률(기본 10% 고정).
- **산식**:
  1. 초과이익 = `지배주주자본 * (예상 ROE - 요구수익률)`
  2. 기업가치(적정가) = `지배주주자본 + (초과이익 / 요구수익률)`
  3. 적정주가 = `기업가치 / 유통주식수`
- **시나리오**: 초과이익 지속계수(w)에 따라 `w=1.0`(유지), `w=0.9`(10% 감소), `w=0.8`(20% 감소)로 구분하여 하락장 방어 가격 산출.

## 5. UI 및 데이터 바인딩 명세
- 데이터가 존재하지 않거나(`NaN`) 계산 불가능한 경우 JSON 상에서 `null`로 직렬화하여 ECharts가 해당 구간을 자연스럽게 건너뛰거나(라인 차트) 비워두도록(막대 차트) 강제함 (숫자 0 강제 치환 금지).
- 숨겨진 탭(Tab) 활성화 시 ECharts 렌더링 찌그러짐(0px) 방지를 위해 `window.dispatchEvent(new Event('resize'))` 트리거.
